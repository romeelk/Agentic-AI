# ADK sample using runner class

This simple example shows how to use ADK runner to orchestrate a simple
chat agent. It also makes use of InMemorySessionService.

## Running the example

As this uses native ADK classes the adk run cli command will not run.

To run this example you just invoke the agent.py with Python:

```
python3 agent.py
```